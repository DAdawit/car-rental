export type Review = {
  Name: string;
  review: string;
  image: string;
};

export type Reviews = Review[];

export type News = {
  title: string;
  image: string;
  detail: string;
  date: string;
};

export type Newses = News[];
